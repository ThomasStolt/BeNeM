# Xcode
DerivedData/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
*.xcuserstate
*.xcscmblueprint

# Build
build/
*.ipa
*.dSYM.zip
*.dSYM

# CocoaPods
Pods/
Podfile.lock

# Swift Package Manager
.build/
.swiftpm/

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Credentials / Secrets
*.env
secrets.plist
ios/BeNeM/Secrets.swift

# Local build configuration (contains device UDID and personal paths — never commit)
build.local.sh

# Local deploy script (contains server hostname — never commit)
deploy.sh

# Personal screenshots accidentally committed — stop tracking future changes
M1BJKI1M3C52.png

# Node (root-level tooling)
node_modules/

# Firecrawl research / scraping cache
.firecrawl/

# Claude Code
.claude/
.superpowers/
.worktrees/

# --- Monorepo subproject ignores ---

# Xcode (scoped to ios/)
ios/**/*.xcuserstate
ios/**/xcuserdata/
ios/DerivedData/
ios/.build/

# Python / FastAPI middleware
middleware/__pycache__/
middleware/**/__pycache__/
middleware/*.pyc
middleware/.env
middleware/venv/
middleware/.venv/

# Node / PWA (future)
pwa/node_modules/
pwa/dist/
pwa/.env

# Secrets — never commit (repo-wide)
*.p8
*.pem
vapid_private_key*
